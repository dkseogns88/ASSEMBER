syntax = "proto3";
package Protocol;

enum PlayerType
{
	PLAYER_TYPE_NONE	= 0;
	PLAYER_TYPE_RINTY	= 1;
	PLAYER_TYPE_SIDA	= 2;
}

enum MoveState
{
	MOVE_STATE_NONE	= 0;
	MOVE_STATE_IDLE	= 1;
	MOVE_STATE_RUN	= 2;
	MOVE_STATE_JUMP	= 3;
}