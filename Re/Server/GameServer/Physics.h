#pragma once

class Physics
{
public:
	
	//static bool LineCollisionWithOBB(const Vector3 obbVertices[8], const Line& line)
	//{
 //       for (int i = 0; i < 8; ++i) {
 //           float dotProduct = (obbVertices[i].x - line.origin.x) * line.direction.x +
 //               (obbVertices[i].y - line.origin.y) * line.direction.y +
 //               (obbVertices[i].z - line.origin.z) * line.direction.z;

 //           // 내적이 0 이상이면 충돌
 //           if (dotProduct >= 0)
 //               return true;
 //       }

 //       return false;
	//}


};

